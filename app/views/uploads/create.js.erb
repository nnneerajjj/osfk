<% if @upload.new_record? %>
  alert("<%= j @upload.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#<%= params[:unique_id] %>")
    .attr('data-form', 'image_upload_ids')
    .attr('data-value', <%= @upload.id %>)
    .html('<img src="<%= @upload.file.url(:thumb) %>">')
<% end %>

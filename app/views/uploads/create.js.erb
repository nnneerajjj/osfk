<% if @upload.new_record? %>
  alert("<%= j @upload.errors.full_messages.join(', ').html_safe %>");
<% else %>
  console.log("<%= @upload.file.url %>");
  console.log("<%= @upload.file.url %>");
  console.log("<%= @upload.file.url %>");
  console.log("<%= @upload.file.url %>");
  console.log("<%= @upload.file.url %>");
<% end %>
